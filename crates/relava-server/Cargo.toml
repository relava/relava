[package]
name = "relava-server"
description = "HTTP/API server for Relava — serves the web UI and wraps relava-core"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
axum.workspace = true
tokio.workspace = true
relava-core.workspace = true

[dev-dependencies]
tower = { version = "0.5", features = ["util"] }
